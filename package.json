{
  "name": "study-planner-full",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && node server.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    
    "railway-install": "cd server && npm install && cd ../client && npm install",
    "railway-build": "cd client && npm run build",
    "railway-start": "cd server && node server.js",
    
    "setup-db": "cd server && node database/init.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}